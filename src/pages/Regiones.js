import React, { useState } from 'react';

const Regiones = () => {
  const [regiones, setRegiones] = useState([]);
  return (
    <div>
      <h1>Regiones</h1>
    </div>
  );
};

export default Regiones;
